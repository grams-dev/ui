import React, {
    useEffect,
    useState
} from "react";

import {
    Button
} from "semantic-ui-react";

import { EditContactProps } from "./Contacts.types";

import { useInverted } from "../../hooks";

/**
 * Edit Contact Component
 */
const EditContact: React.FC<EditContactProps> = ({
}) => {

    return (
        <div />
    );
}

export default EditContact;
