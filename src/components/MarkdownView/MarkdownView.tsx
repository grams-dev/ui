import React, {
  useState
} from "react";

import { MarkdownViewProps } from "./MarkdownView.types";

/**
 * A component description
 */
const MarkdownView: React.FC<MarkdownViewProps> = ({
}) => {
  return (
    <div
      data-testid="MarkdownView"
    >
    </div>
  );
}

export default MarkdownView;
