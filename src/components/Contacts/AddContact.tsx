import React, {
    useEffect,
    useState
} from "react";

import {
    Button
} from "semantic-ui-react";

import { AddContactProps } from "./Contacts.types";

import { useInverted } from "../../hooks";

/**
 * Add Contact Component
 */
const AddContact: React.FC<AddContactProps> = ({
}) => {

    return (
        <div />
    );
}

export default AddContact;
