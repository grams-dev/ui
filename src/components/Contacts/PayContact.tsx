import React, {
    useEffect,
    useState
} from "react";

import {
    Button
} from "semantic-ui-react";

import { PayContactProps } from "./Contacts.types";

import { useInverted } from "../../hooks";

/**
 * Add Contact Component
 */
const PayContact: React.FC<PayContactProps> = ({
}) => {

    return (
        <div />
    );
}

export default PayContact;
