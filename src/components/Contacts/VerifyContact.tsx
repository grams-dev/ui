import React, {
    useEffect,
    useState
} from "react";

import {
    Button
} from "semantic-ui-react";

import { VerifyContactProps } from "./Contacts.types";

import { useInverted } from "../../hooks";

/**
 * Add Contact Component
 */
const VerifyContact: React.FC<VerifyContactProps> = ({
}) => {

    return (
        <div />
    );
}

export default VerifyContact;
