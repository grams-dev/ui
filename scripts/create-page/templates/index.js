const page = require("./page");
const pageStyles = require("./page.css");
const pageStories = require("./page.stories");
const pageTests = require("./page.test");
const pageTypes = require("./page.types");

module.exports = [
  page,
  pageStyles,
  pageStories,
  pageTests,
  pageTypes
];
