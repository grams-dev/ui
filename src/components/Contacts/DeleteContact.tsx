import React, {
    useEffect,
    useState
} from "react";

import {
    Button
} from "semantic-ui-react";

import { DeleteContactProps } from "./Contacts.types";

import { useInverted } from "../../hooks";

/**
 * Delete Contact Component
 */
const DeleteContact: React.FC<DeleteContactProps> = ({
}) => {

    return (
        <div />
    );
}

export default DeleteContact;
